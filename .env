# Local storage — no Docker/MySQL required
DB_BACKEND=sqlite

# Optional MySQL (only if Docker is installed): set DB_BACKEND=mysql
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=budget
MYSQL_PASSWORD=budget
MYSQL_DATABASE=budget_app
