# sqlite = default, works without Docker (data in data/budget.db)
# mysql = requires docker compose up -d
DB_BACKEND=sqlite

MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=budget
MYSQL_PASSWORD=budget
MYSQL_DATABASE=budget_app
